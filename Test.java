package org.apache.nicolasBecasPI;




import java.io.IOException;

import org.apache.sanselan.ImageReadException;

public class Test {

	public static void main(String[] args) throws IOException, ImageReadException {
		
		

		/**
		 * 
   	    String directorioActual = System.getProperty("user.dir");
        String rutaRaiz = directorioActual + File.separator + "Album";
        
   	 
       DirectoryGenerator.generateDirectoryStructureWithImages(2, 2,directorioActual, 4);

       // Procesar imágenes en la carpeta
       File folder = new File(rutaRaiz);
      List<MetadatosImagen> metadatosImagenList = MetadataEditor.processImagesInFolder(folder);
		
     metadatosImagenList2 =  MetadataEditor.filterByUbicacion(metadatosImagenList,"Zaragoza");
      
      for(MetadatosImagen met : metadatosImagenList2) {
    	  met.imprimirMetadatos();
      }*/
		 
	
	}

}
